name: Deploy to Vercel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: team_XeusBqCOFpczA3lK19IgZB6x
          vercel-project-id: prj_IhI9nKQHzBKQ1gwcSmMvpdAXOA33
          vercel-args: '--prod'

        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
